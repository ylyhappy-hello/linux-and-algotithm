#include <algorithm>
#include <cstring>
#include <iostream>
#include <queue>
#include <stack>
#include <unordered_map>
#include <vector>
using namespace std;
typedef long long ll;
typedef pair<int, int> PII;
const int N = 1e+5 + 10;

int quicksort(int l, int r, int q[]) {
  
}
int main() {
  ios::sync_with_stdio(
      false);  // 提高cin速度,但是不可以使用scanf,scanf有时候有问题,还是cin吧...
  cin.tie(nullptr);

  return 0;
}
